# Notes

- User wants GCP taught through concepts they already know from AWS IAM Identity Center, permission sets, permissions boundaries, EC2 instance profiles, ECR auth, and GitHub OIDC roles.
- Immediate learning goal: manage GCP auth safely in the current terminal session before migrating `labs/05-static-site-ec2/` to GCE.
- Prefer best-practice GCP patterns over direct AWS feature-for-feature translation; avoid service account keys.
