# AWS IAM starting point

The user already manages local AWS CLI/Terraform permissions with IAM Identity Center, an `agent` permission set, `PowerUserAccess` plus a scoped inline policy, and permissions boundaries for Terraform-created roles. Future GCP lessons should map concepts from this model instead of teaching cloud IAM from zero.
