apiVersion: v1
kind: ConfigMap
metadata:
  name: synergychat-api-configmap
data:
  API_PORT: "8080"
  API_DB_FILEPATH: "/persist/db.json"
  # namespace also affect how DNS works internally
  CRAWLER_BASE_URL: "http://crawler-service.crawler.svc.cluster.local"
