# Copy to backend.hcl and adjust if your state bucket/table names differ.
# This mirrors the remote-state naming pattern from ../01-single-web-server.
bucket         = "noidilin-tf-state"
key            = "labs/05-static-site-ec2/infra/stage/terraform.tfstate"
region         = "ap-northeast-1"
use_lockfile   = true
encrypt        = true
