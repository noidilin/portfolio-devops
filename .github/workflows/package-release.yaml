name: package-release

on:
  push:
    paths:
      - ".github/workflows/package-release.yaml"
      - "labs/00-basic-docker-in-a-month-of-lunches/ch16/cicd/tag-push.ps1"
      - "labs/00-basic-docker-in-a-month-of-lunches/ch16/cicd/set-vars.ps1"
      - "labs/00-basic-docker-in-a-month-of-lunches/ch16/src/docker-compose*.yml"
  workflow_run:
    workflows: [smoke-test]
    types: [completed]
    branches:
      - "main"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  REPOSITORY: ${{ github.repository_owner }}

jobs:
  package:
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@master
      - name: GHCR login
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: push-release
        working-directory: labs/00-basic-docker-in-a-month-of-lunches/ch16/cicd
        run: ./tag-push.ps1 -From staging -To release
        shell: pwsh
    permissions:
      contents: read
      packages: write

